<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SK Mini Editor - Editor de Código Online</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --bg-primary: #0d1117;
      --bg-secondary: #161b22;
      --bg-tertiary: #21262d;
      --border: #30363d;
      --text-primary: #c9d1d9;
      --text-secondary: #8b949e;
      --accent: #1f6feb;
      --accent-hover: #388bfd;
      --success: #238636;
      --danger: #da3633;
    }

    html, body {
      width: 100%;
      height: 100%;
      overflow: hidden;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
      background: var(--bg-primary);
      color: var(--text-primary);
    }

    .app-container {
      display: flex;
      flex-direction: column;
      width: 100%;
      height: 100%;
    }

    .app-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.75rem 1rem;
      background: var(--bg-secondary);
      border-bottom: 1px solid var(--border);
      gap: 1rem;
      flex-wrap: wrap;
    }

    .header-left {
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }

    .header-left i {
      font-size: 1.5rem;
      color: #58a6ff;
    }

    .header-left h1 {
      font-size: 1.1rem;
      font-weight: 700;
    }

    .header-center {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      flex: 1;
      justify-content: center;
    }

    .project-name-input {
      padding: 0.5rem 0.75rem;
      background: var(--bg-primary);
      border: 1px solid var(--border);
      border-radius: 0.375rem;
      color: var(--text-primary);
      font-size: 0.875rem;
      outline: none;
      min-width: 150px;
    }

    .project-name-input:focus {
      border-color: var(--accent);
    }

    .header-right {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .btn-header {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 0.75rem;
      background: var(--accent);
      border: none;
      border-radius: 0.375rem;
      color: white;
      cursor: pointer;
      font-size: 0.875rem;
      font-weight: 600;
      transition: all 0.2s;
      white-space: nowrap;
    }

    .btn-header:hover {
      background: var(--accent-hover);
    }

    .btn-header.secondary {
      background: var(--bg-tertiary);
      color: var(--text-primary);
      border: 1px solid var(--border);
    }

    .btn-header.secondary:hover {
      background: rgba(255, 255, 255, 0.1);
    }

    .app-main {
      display: flex;
      flex: 1;
      overflow: hidden;
      gap: 1px;
      background: var(--border);
    }

    .sidebar {
      width: 280px;
      background: var(--bg-secondary);
      display: flex;
      flex-direction: column;
      overflow: hidden;
      border-right: 1px solid var(--border);
    }

    .sidebar-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.75rem 1rem;
      border-bottom: 1px solid var(--border);
      background: var(--bg-tertiary);
    }

    .sidebar-header h2 {
      font-size: 0.875rem;
      font-weight: 600;
      text-transform: uppercase;
      color: var(--text-secondary);
    }

    .sidebar-header button {
      background: none;
      border: none;
      color: var(--text-secondary);
      cursor: pointer;
      font-size: 1rem;
      padding: 0.25rem;
    }

    .sidebar-header button:hover {
      color: var(--text-primary);
    }

    .filetree-container {
      flex: 1;
      overflow-y: auto;
      overflow-x: hidden;
      padding: 0.5rem 0;
    }

    .tree-node {
      display: flex;
      flex-direction: column;
    }

    .tree-item {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 0.75rem;
      cursor: pointer;
      user-select: none;
      transition: all 0.15s;
      border-left: 2px solid transparent;
    }

    .tree-item:hover {
      background: rgba(255, 255, 255, 0.05);
    }

    .tree-item.active {
      background: rgba(31, 111, 235, 0.15);
      border-left-color: var(--accent);
      color: #58a6ff;
    }

    .tree-toggle {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 1.25rem;
      height: 1.25rem;
      background: none;
      border: none;
      color: var(--text-secondary);
      cursor: pointer;
      padding: 0;
      font-size: 0.75rem;
    }

    .tree-toggle-placeholder {
      width: 1.25rem;
      height: 1.25rem;
    }

    .tree-icon {
      font-size: 0.875rem;
      width: 1rem;
      text-align: center;
    }

    .tree-icon.folder {
      color: #d4a574;
    }

    .tree-icon.file {
      color: #79c0ff;
    }

    .tree-icon.file.html { color: #fb8500; }
    .tree-icon.file.css { color: #b77dff; }
    .tree-icon.file.js { color: #ffd60a; }
    .tree-icon.file.json { color: #52b788; }
    .tree-icon.file.image { color: #52b788; }
    .tree-icon.file.markdown { color: #8b949e; }
    .tree-icon.file.text { color: #8b949e; }

    .tree-name {
      flex: 1;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      font-size: 0.875rem;
    }

    .tree-menu {
      opacity: 0;
      background: none;
      border: none;
      color: var(--text-secondary);
      cursor: pointer;
      padding: 0.25rem;
      font-size: 0.75rem;
    }

    .tree-item:hover .tree-menu {
      opacity: 1;
    }

    .tree-children {
      display: flex;
      flex-direction: column;
    }

    .editor-area {
      flex: 1;
      display: flex;
      flex-direction: column;
      overflow: hidden;
      background: var(--bg-primary);
    }

    .editor-tabs {
      display: flex;
      gap: 0.5rem;
      padding: 0.5rem 0.75rem;
      background: var(--bg-secondary);
      border-bottom: 1px solid var(--border);
      overflow-x: auto;
      overflow-y: hidden;
      align-items: center;
    }

    .tab {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 0.75rem;
      background: var(--bg-tertiary);
      border: 1px solid var(--border);
      border-radius: 0.375rem 0.375rem 0 0;
      cursor: pointer;
      font-size: 0.875rem;
      white-space: nowrap;
      transition: all 0.2s;
      color: var(--text-secondary);
    }

    .tab:hover {
      background: rgba(255, 255, 255, 0.05);
    }

    .tab.active {
      background: var(--accent);
      border-color: var(--accent);
      color: white;
      font-weight: 600;
    }

    .tab-close {
      background: none;
      border: none;
      color: inherit;
      cursor: pointer;
      padding: 0;
      font-size: 0.75rem;
      opacity: 0.7;
      display: flex;
      align-items: center;
      justify-content: center;
      width: 1rem;
      height: 1rem;
    }

    .tab-close:hover {
      opacity: 1;
    }

    .editor-content {
      display: flex;
      flex: 1;
      overflow: hidden;
      gap: 1px;
      background: var(--border);
    }

    .code-editor-section {
      flex: 1;
      display: flex;
      flex-direction: column;
      overflow: hidden;
    }

    .code-editor {
      flex: 1;
      padding: 1rem;
      background: var(--bg-primary);
      color: var(--text-primary);
      border: none;
      outline: none;
      font-family: 'Fira Code', 'Courier New', monospace;
      font-size: 0.9rem;
      line-height: 1.6;
      resize: none;
      tab-size: 2;
      overflow-y: auto;
    }

    .code-editor::selection {
      background: rgba(31, 111, 235, 0.5);
    }

    .code-editor::-webkit-scrollbar {
      width: 8px;
    }

    .code-editor::-webkit-scrollbar-thumb {
      background: var(--border);
      border-radius: 4px;
    }

    .preview-section {
      width: 50%;
      display: flex;
      flex-direction: column;
      background: white;
      overflow: hidden;
      border-left: 1px solid var(--border);
    }

    .preview-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.75rem 1rem;
      background: #f6f8fa;
      border-bottom: 1px solid #d0d7de;
      font-size: 0.875rem;
      font-weight: 600;
      color: #24292f;
    }

    .preview-content {
      flex: 1;
      overflow: auto;
      background: white;
    }

    .preview-frame {
      width: 100%;
      height: 100%;
      border: none;
      background: white;
    }

    .preview-image {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      height: 100%;
      background: #f6f8fa;
    }

    .preview-image img {
      max-width: 100%;
      max-height: 100%;
      object-fit: contain;
    }

    .preview-text {
      padding: 1rem;
      overflow: auto;
      background: white;
      font-family: 'Courier New', monospace;
      font-size: 0.875rem;
      white-space: pre-wrap;
      word-wrap: break-word;
      color: #24292f;
      line-height: 1.5;
    }

    .bottom-sheet {
      position: fixed;
      inset: 0;
      z-index: 9999;
      display: flex;
      flex-direction: column;
      animation: slideUp 0.3s ease-out;
    }

    @keyframes slideUp {
      from {
        opacity: 0;
        transform: translateY(100px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .sheet-overlay {
      flex: 1;
      background: rgba(0, 0, 0, 0.5);
      cursor: pointer;
    }

    .sheet-content {
      background: var(--bg-secondary);
      border-top: 1px solid var(--border);
      border-radius: 1.5rem 1.5rem 0 0;
      overflow: hidden;
      max-height: 80vh;
      display: flex;
      flex-direction: column;
    }

    .sheet-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 1rem;
      border-bottom: 1px solid var(--border);
      background: var(--bg-tertiary);
    }

    .sheet-header h3 {
      margin: 0;
      font-size: 1rem;
      font-weight: 600;
    }

    .sheet-header button {
      background: none;
      border: none;
      color: var(--text-secondary);
      cursor: pointer;
      font-size: 1.25rem;
      padding: 0;
    }

    .sheet-items {
      display: flex;
      flex-direction: column;
      overflow-y: auto;
      padding-bottom: 2rem;
    }

    .sheet-item {
      display: flex;
      align-items: center;
      gap: 1rem;
      padding: 1rem;
      background: none;
      border: none;
      color: var(--text-primary);
      cursor: pointer;
      text-align: left;
      font-size: 0.875rem;
      transition: background 0.2s;
      border-left: 3px solid transparent;
    }

    .sheet-item:hover {
      background: rgba(255, 255, 255, 0.05);
      border-left-color: var(--accent);
    }

    .sheet-item.danger {
      color: #f85149;
    }

    .sheet-item i {
      font-size: 1.1rem;
      width: 1.5rem;
      text-align: center;
    }

    ::-webkit-scrollbar {
      width: 8px;
      height: 8px;
    }

    ::-webkit-scrollbar-track {
      background: transparent;
    }

    ::-webkit-scrollbar-thumb {
      background: var(--border);
      border-radius: 4px;
    }

    @media (max-width: 1200px) {
      .preview-section {
        display: none;
      }
    }

    @media (max-width: 768px) {
      .sidebar {
        width: 200px;
      }
    }

    #importInput {
      display: none;
    }

    .hidden {
      display: none !important;
    }
  </style>
</head>
<body>
  <div class="app-container">
    <header class="app-header">
      <div class="header-left">
        <i class="fas fa-code"></i>
        <h1>SK Mini Editor</h1>
      </div>

      <div class="header-center">
        <input 
          type="text" 
          id="projectName" 
          class="project-name-input" 
          placeholder="Nome do projeto"
          value="Meu Projeto"
        >
      </div>

      <div class="header-right">
        <button id="importBtn" class="btn-header" title="Importar">
          <i class="fas fa-upload"></i>
          <span>Importar</span>
        </button>
        <button id="exportBtn" class="btn-header secondary" title="Exportar">
          <i class="fas fa-download"></i>
          <span>Exportar</span>
        </button>
        <button id="githubBtn" class="btn-header secondary" title="GitHub">
          <i class="fab fa-github"></i>
          <span>GitHub</span>
        </button>
      </div>
    </header>

    <div class="app-main">
      <aside class="sidebar">
        <div class="sidebar-header">
          <h2>📁 Arquivos</h2>
          <button id="newFileBtn" title="Novo arquivo">
            <i class="fas fa-plus"></i>
          </button>
        </div>
        <div id="fileTree" class="filetree-container"></div>
      </aside>

      <main class="editor-area">
        <div class="editor-tabs" id="editorTabs"></div>
        <div class="editor-content">
          <div class="code-editor-section">
            <textarea 
              id="codeEditor" 
              class="code-editor" 
              spellcheck="false"
              placeholder="Selecione um arquivo..."
            ></textarea>
          </div>
          <div class="preview-section">
            <div class="preview-header">
              <span>📺 Preview</span>
            </div>
            <div class="preview-content" id="previewContent">
              <iframe id="previewFrame" class="preview-frame"></iframe>
              <div id="previewImage" class="preview-image hidden"></div>
              <div id="previewText" class="preview-text hidden"></div>
            </div>
          </div>
        </div>
      </main>
    </div>
  </div>

  <input type="file" id="importInput" accept=".zip,.txt,.html,.css,.js,.json,.md,.py,.rb,.go,.rs,.java,.xml,.yaml,.yml,.ts,.jsx,.tsx,.scss">

  <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/file-saver/2.0.5/FileSaver.min.js"></script>
  
  <script src="js/mini-vfs.js"></script>
  <script src="js/mini-filetree.js"></script>
  <script src="js/mini-github.js"></script>
  <script src="js/mini-editor.js"></script>
</body>
</html>
